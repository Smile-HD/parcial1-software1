package {{packageName}};

{{#each (methodImports methods)}}
import {{this}};
{{/each}}

/**
 * Interfaz Java generada para el clasificador de interfaz UML {{className}}.
 * Renderizado por templates/spring-backend/interface.hbs — no editar manualmente.
 *
 * Mapeo 14c: un clasificador de interfaz produce únicamente una interfaz Java simple —
 * sin entidad JPA, repositorio, controlador ni servicio (decisión documentada:
 * las interfaces representan comportamiento, no persistencia). Las clases que la implementan
 * reciben una cláusula `implements` en su entidad.
 */
public interface {{className}} {
{{#each methods}}
    {{returnType}} {{name}}({{#each parameters}}{{type}} {{name}}{{#unless @last}}, {{/unless}}{{/each}});
{{/each}}
}
