package {{basePackage}};

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Punto de entrada generado de Spring Boot — necesario para que spring-boot-maven-plugin
 * genere un jar ejecutable (la verificación golden ejecuta `java -jar target/*.jar`).
 * Renderizado por templates/spring-backend/application.hbs — no editar manualmente.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
