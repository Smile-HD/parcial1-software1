package {{basePackage}};

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Generated Spring Boot entry point — required so spring-boot-maven-plugin
 * produces an executable jar (golden check runs `java -jar target/*.jar`).
 * Rendered by templates/spring-backend/application.hbs — do not edit by hand.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
