# Configuración de Spring generada — modo archivo H2 (decisión de diseño 10:
# los datos persisten entre reinicios en ./data, ideal para uso sin conexión).
# Renderizado por templates/spring-backend/application-properties.hbs — no editar manualmente.
spring.application.name=generated-backend
server.port=8080

spring.datasource.url=jdbc:h2:file:./data/generated-db
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# 18: Asistente offline (diseño D11 — qwen2.5:1.5b en Ollama localhost)
assistant.model={{assistantModel}}
assistant.ollama.url={{assistantOllamaUrl}}
assistant.auto-pull=false
