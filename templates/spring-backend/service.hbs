package {{packageName}}.service;

import {{packageName}}.{{className}};
import {{packageName}}.repository.{{className}}Repository;
import jakarta.persistence.EntityNotFoundException;
import java.util.List;
import org.springframework.stereotype.Service;

/**
 * Capa de servicio delgada generada para {{className}} (decisión del mantenedor
 * 2026-09-07 — arquitectura 3+1): inyecta por constructor el repositorio
 * Spring Data y expone operaciones CRUD directas. Los controladores dependen
 * de este servicio, nunca del repositorio directamente. Las interfaces no tienen servicio.
 * Renderizado por templates/spring-backend/service.hbs — no editar manualmente.
 */
@Service
public class {{className}}Service {

    private final {{className}}Repository repository;

    public {{className}}Service({{className}}Repository repository) {
        this.repository = repository;
    }

    public List<{{className}}> findAll() {
        return repository.findAll();
    }

    public {{className}} findById(Long id) {
        return repository.findById(id)
                .orElseThrow(() -> new EntityNotFoundException(
                        "{{className}} " + id + " not found"));
    }

    public {{className}} save({{className}} entity) {
        return repository.save(entity);
    }

    public void deleteById(Long id) {
        if (!repository.existsById(id)) {
            throw new EntityNotFoundException("{{className}} " + id + " not found");
        }
        repository.deleteById(id);
    }
}
