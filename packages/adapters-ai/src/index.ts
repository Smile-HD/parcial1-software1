/**
 * Placeholder entry point for @app/adapters-ai.
 * LlmPort / SttPort / VisionPort adapters (OpenAI-compatible + fakes)
 * land in PRs 7-8 (Phase 4). Nothing beyond this placeholder yet.
 */
export const PACKAGE_NAME = '@app/adapters-ai' as const;
